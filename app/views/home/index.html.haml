.text-center.justify-content-center.container
  %h1 Welcome #{current_user.name}, to your Memories Dashboard
  %p Below are your saved memories arranged by folder.

  .header
    %p.font-bold My Journals
    = link_to "New Journal", new_journal_path, class: "px-3 py-2 bg-green-600 text-white font-medium text-xs leading-tight rounded shadow-md hover:bg-green-700 hover:shadow-lg focus:bg-green-700 focus:shadow-lg focus:outline-none focus:ring-0 active:bg-green-800 active:shadow-lg transition duration-150 ease-in-out", data: {turbo_frame: dom_id(@journal)}

  = turbo_frame_tag dom_id(@journal)

  = turbo_stream_from current_user, :journals
  .journal-list
    %ul#journals
      = render @user_journals