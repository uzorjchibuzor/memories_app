= turbo_frame_tag dom_id(entry) do 
  - if entry.date 
    .entry-div.card-it.mb-7
      .italic.flex.flex-wrap.justify-between.p-2.bg-neutral-400.text-white
        %p= entry.date.strftime('%B %d, %Y') 
        %div.flex
          .rounded.border.bg-blue-400.px-3.py-1.text-xs.mx-2
            = link_to 'Edit', [:edit, @journal, entry], data: { turbo_frame: dom_id(entry) }
          .rounded.border.bg-red-400.px-3.py-1.text-xs
            = link_to 'Delete', [@journal, entry], data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }
      .max-w-xl.ml-mr-auto.bg-blue-200.p-3.text-justify= entry.thoughts